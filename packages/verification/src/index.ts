/**
 * @bridgewise/verification — On-chain light client header and state verification engine.
 *
 * @example
 * ```ts
 * import { HeaderVerifier, SyncCommitteeVerifier } from '@bridgewise/verification';
 * import type { BlockHeader, SyncCommittee, StateProof } from '@bridgewise/verification';
 * ```
 */

export * from "./light-client";
